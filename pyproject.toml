[project]
name = "mcp-jokes-server"
version = "0.1.0"
description = "Tiny remote MCP server with adult-joke and GenZ-word tools."
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.2.0",
    "streamlit>=1.56.0",
    "uvicorn>=0.30",
]

[tool.uv]
package = false
